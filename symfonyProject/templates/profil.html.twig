<!DOCTYPE html>

<head xmlns="http://www.w3.org/1999/html">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <script src="	https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <title></title>
    <style>

        body {
            background:#77b38f;
        }

        .form-control:focus {
            box-shadow: none;
            border-color: #BA68C8
        }

        .profile-button {
            background: rgb(99, 39, 120);
            box-shadow: none;
            border: none
        }

        .profile-button:hover {
            background: #682773
        }

        .profile-button:focus {
            background: #6188a4;
            box-shadow: none
        }

        .profile-button:active {
            background: #6188a4;
            box-shadow: none
        }

        .back:hover {
            color: #6188a4;
            cursor: pointer
        }

        .labels {
            font-size: 11px
        }

        .add-experience:hover {
            background: #6188a4;
            color: #fff;
            cursor: pointer;
            border: solid 1px #BA68C8
        }
    </style>
</head>
<body>
<div class="container rounded bg-white mt-5 mb-5">
    <div class="row">
        <div class="col-md-3 border-right">



            <ul class="nav justify-content-end">
                <li class="nav-item">
                    <a class="nav-link active" href="#">Profil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('accueil') }}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">DECONNEXION</a>
                </li>

            </ul>
            <div class="d-flex flex-column align-items-center text-center p-3 py-5">
                <div class="col-xs4">
                    <h2>Profil : </h2>
                </div>
                <img class="rounded-circle mt-5" width="150px" src="{{ asset('uploads/image/' ~ app.user.image) }}">

            </div>


        </div>
        <div class="col-md-5 border-right">

            {% block body %}

            {% endblock %}
        </div>

        <div class="col-md-4">
            <div class="p-3 py-5">

                <div class="d-flex justify-content-between align-items-center experience">
                    <span class="border px-3 p-1 add-experience"><i class="fa fa-plus"></i>&nbsp;Paramétres</span></div><br>

                <ul class="nav flex-column">
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('utilisateur_show' ,{'id':app.user.id})}}">Profil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('utilisateur_edit',{'id':app.user.id}) }}">Modifier Profil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('users_pass_modifier',{'id':app.user.id}) }}">Modifier le mot de passe</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('utilisateur_delete',{'id':app.user.id}) }}">Supprimer mon compte</a>
                    </li>
                </ul>


            </div>



        </div>
    </div>
</div>
</div>
</div>
</div>


</body>
</html>